
const Footer = (props) => {

    return (

        <div>
            <h1>Footer Component</h1>
            <p>Parent data in Child: {props.dataToPass}</p>

        </div>


    );
}

export default Footer;
